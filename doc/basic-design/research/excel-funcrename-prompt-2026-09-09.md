# Excel 反映指示 — 機能グループ・機能名の見直し（2026-09-09）

以下をそのまま Excel 担当（エージェント）へ渡す。

---

## 依頼

`doc/excel/認証タスク.xlsx` の **7 シートを全面置換**してください。機能グループと機能名の名前を見直し、呼び出し口の違いだけで分かれていた機能を統合しました。**人日と行数は 1 つも動いていません。**

- **根拠**: [function-naming-review-2026-09-09.md](function-naming-review-2026-09-09.md)（見直しの原則と、名前ごとの理由）
- **変更の一覧**: `wbs-text/SYNC_funcrename_2026-09-09.tsv`（**60 件**。旧 → 新の対応表。確認用で、作業には使わない）
- **入力**: `wbs-text/SHEET_*.tsv`（UTF-8・タブ区切り・1 行目ヘッダ・列順は Excel と同じ）
- **原則**: 全面置換。**列の増減はなし。書式・条件付き書式・ウィンドウ枠の固定はそのまま維持する**

## 1. 何が変わったか（3 行で）

1. **機能グループが 24 → 20**（機能系のみ。非機能 9 は変更なし。全体では 33 → 29）。廃止 4・改名 8
2. **機能名が 97 → 88**（機能系のみ。非機能 43 は変更なし。全体では 140 → 131）。**統合 9・改名 30**
3. **人日は一切変わらない。** ① 233.5 / ② 184 / ③ 276.5 / ④ 345 / 合計 1026

## 2. 全面置換するシート（7 つ）

| シート | TSV | 行数 | 変わったもの |
|---|---|---:|---|
| WBS(基本設計) | `SHEET_基本設計_1.tsv` ＋ `_2.tsv` | 158 | 機能グループ・機能名の列。**並び順も新しいグループ順に変わる** |
| WBS(詳細設計_製造) | `SHEET_詳細設計_製造.tsv` | 181 | 同上 |
| WBS(テスト) | `SHEET_テスト.tsv` | 94 | 機能グループ・機能名の列（並び順は区分優先のまま） |
| サマリ(工程別) | `SHEET_サマリ工程別.tsv` | **79**（旧 83） | 機能名の統合で 4 行減る。**合計 1026 は不変** |
| 機能名一覧 | `SHEET_機能名一覧.tsv` | **131**（旧 140） | 統合で 9 行減る |
| 機能グループ一覧 | `SHEET_機能グループ一覧.tsv` | **29**（旧 33） | 統合で 4 行減る |
| 提供API一覧 | `SHEET_API提供一覧.tsv` | 108 | 機能グループ・機能名・機能IDの列 |

**変更しないシート**: 要件マッピング（209 行）・コスト見積もり・設計決定一覧・ADR一覧・運用（定常）。**要件マッピングは機能名の列を持たないため影響なし。**

**要件・機能対応表**（作っている場合）は機能名を持つので、`SHEET_要件機能マッピング_1.tsv` ＋ `_2.tsv`（**312 行**、旧 315）で置き換えてください。

## 3. 主な改名（覚えておくと確認が早い）

**機能グループ**

| 旧 | 新 |
|---|---|
| ログイン振り分け（HRD） | ログインの振り分け |
| フェデレーションログイン | 顧客IdP 連携ログイン |
| ローカルログイン（Keycloak-IdP 収容） | パスワードによるログイン |
| 不正アクセス対策 | ログイン口の保護 |
| ユーザ登録（顧客システム連携・SCIM） | **廃止 → ユーザ管理へ** |
| ユーザ管理（管理画面・API） | ユーザ管理 |
| 同意管理 | **廃止 → ログイン画面へ** |
| 権限管理 | **廃止 → 認可・権限判定へ** |
| データ整合・突合 | **廃止 → ユーザの停止・再開・消去へ** |
| JWT の発行・交換 | JWT の発行 |
| アプリ・外部連携 | アプリ連携 |
| 業務システム連携（ServiceNow） | 業務システムへの認証提供 |

**機能名（統合されたもの＝行が減る 9 件）**

| 旧（複数） | 新（1 つ） |
|---|---|
| SCIM 受信: ユーザの登録 / 更新 / 削除通知 / 検索 / グループ連携 | **顧客IdP からのユーザ情報の同期** |
| 追加認証（数字コード）/（生体・セキュリティキー） | **追加認証の利用** |
| 追加認証の登録（数字コード）/（生体・セキュリティキー） | **追加認証の登録** |
| ユーザの検索・一覧 / 詳細参照 | **ユーザの参照** |
| ユーザの登録 / 変更 | **ユーザの登録と変更** |
| ユーザの利用可否の切替 / 削除 | **ユーザの利用可否の切替・削除** |

**機能名（主語を足した・言い換えた 主なもの）**

| 旧 | 新 |
|---|---|
| JWT に載せる項目の定義 | **アプリへの JWT 発行**（グループも「JWT の発行」へ移動） |
| JWT の交換 | 別アプリ向け JWT への交換（Token Exchange） |
| 証明書期限の監視 | 顧客IdP・本基盤の証明書の期限監視 |
| 署名鍵の定期入れ替え | JWT 署名鍵の定期入れ替え |
| 接続用パスワードの定期入れ替え | アプリの接続用パスワードの定期入れ替え |
| 項目名の変換・統一 | 顧客IdP から受け取る属性の名前の統一 |
| 接続情報の自動追随 | 顧客IdP の接続情報の自動追随 |
| 監査ログの取得・保管方式 | 監査ログの出力と保管 |
| 管理 API の内部公開（idm-api 向け） | アプリ向け管理 API の公開 |
| 不正兆候の検知 | 不正なログインの兆候の検知 |

> **Keycloak-Broker / Keycloak-IdP という製品名は機能名にそのまま残しています**（設計者向けに具体的な方がよいという判断）。`Keycloak-IdP 収容ユーザのログイン` などは変わりません。

## 4. 検収

1. **人日が 1 つも動いていない**: ① 全量 233.5 / 対象 220.5、② 184、③ 276.5、④ 345、サマリ合計 1026
2. **行数**: ① 158 / ②③ 181 / ④ 94（**この 3 つは変わらない**）、サマリ 79 / 機能名一覧 131 / 機能グループ一覧 29 / 提供API一覧 108
3. **機能グループ・機能名が、4 シート（基本設計 / 詳細設計_製造 / テスト / サマリ）とも 機能名一覧に存在する**。存在しないものは 0 件
4. 機能名一覧の 行数 / 人日 / うち対象 が WBS(基本設計) の集計と一致する
5. **旧い名前が 1 つも残っていない**: 次の 11 語で検索してヒット **0 件**（列の値だけでなく、作業名・概要の本文も含めて確認済み）
   - `SCIM 受信:` / `フェデレーションログイン` / `ユーザ登録（顧客システム連携` / `JWT に載せる項目の定義` / `不正アクセス対策` / `権限管理` / `ログイン振り分け（HRD）` / `ユーザ管理（管理画面・API）` / `業務システム連携（ServiceNow）` / `同意管理` / `データ整合・突合`
6. 要件マッピング（209 行）が**変わっていない**

## 5. 報告してほしいこと

- 検収 1〜6 の結果
- 置換で崩れた書式・数式の場所
- 旧い名前が残っていた場所（あれば）
- 直したくなった箇所は直さず、場所と理由だけ報告する
