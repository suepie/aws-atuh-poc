# Excel 反映指示 — メモ整理（2026-09-06）

- **対象**: `doc/excel/認証タスク.xlsx` シート **WBS(基本設計)**（ヘッダ行 6、キーは A 列 WBS ID）
- **根拠**: [memo-triage-2026-09-06.md](memo-triage-2026-09-06.md)（整理方針）/ [memo-triage-changelog-2026-09-06.md](memo-triage-changelog-2026-09-06.md)（行別の変更 347 件）
- **入力ファイル**: `doc/basic-design/research/wbs-text/SYNC_*.tsv`（すべて UTF-8・タブ区切り・1 行目ヘッダ）
- **列の対応**: `A=ID B=分類 C=作業種別 D=メモ E=項目 F=概要 G=成果物と完了条件 H=人日 I=機能グループ J=機能名 K=担当（案②） L=担当（案③） M=判断根拠 N=根拠の補足 O=スコープ P=当初想定 Q=依存 R=状態 S=出典 T=機能ID`
- **原則**: 列は増やさない（並べ替え用の一時列は最後に削除）。D 列メモは消さずそのまま残す（行ごとの確認に使う）

---

## 手順（この順で）

### 1. 行の削除（20 行）— `SYNC_delete.tsv`

| 種類 | 行 |
|---|---|
| 人日 0 の旧集計行・廃止行 | HB-09〜14 / B-1.3 / D-3 / D-4 / D-10 / A-4（11 行） |
| 分割により子行へ置換する親 | E-4 / HC-01 / HC-05a / HC-05b / HC-05c / HC-05d / HB-F-AZ-05 / HE-22 / HE-18（9 行） |

### 2. 行の追加（33 行）— `SYNC_new_rows.tsv`

親 ID の**元の位置**に挿入する（並び順は手順 8 で最終的に揃うので、位置は暫定でよい）。

| TSV 列 | Excel 列 |
|---|---|
| 項目 | E |
| 作業種別 | C |
| 機能ID | T |
| 機能グループ | I |
| 機能名 | J |
| 担当（案②: 一部移管）/ 担当（案③: アプリ構築） | K / L |
| 判断根拠 / 根拠の補足 | M / N |
| スコープ / 当初想定 | O / P |
| 概要 / 成果物と完了条件 | F / G |
| 状態 | R |
| 人日 | H |
| （TSV に無い列） | B 分類 = 親 ID の分類をそのまま / Q 依存 = 「—」/ S 出典 = 空 |

内訳: E-4a〜e（5）/ HC-01a・b（2）/ HC-05a-1〜6・05b-1〜8・05c-1〜4・05d-1〜2（20）/ HB-F-AZ-05a・b（2）/ HE-22a・b（2）/ HE-18a・b（2）。

### 3. 項目名の変更（149 行）— `SYNC_name.tsv` → E 列

接頭辞（前提：/共通：/認証：/運用設計：/セキュリティ：/構成図：/構成：/調査計画：/HRD：/画面（認証製品内）：/画面（管理画面）：）と改名を含む。新規行の名前も入っている（手順 2 で入れていれば上書きで同じ）。

### 4. 分類値の一括更新（全 463 行）— `SYNC_class.tsv` → C / K / L / M / O / P / R 列

ファイル冒頭の凡例でコードを値に戻す。**今回の変更点**:
- スコープ（O 列）に新値 **4 = 「対象外：省略・吸収」**（60 行）
- **状態（R 列）が追加**された（要件確認 14 行・保留 2 行・着手 3 行。それ以外は現状のまま）
- 担当（K 列）の変更 25 行（ADM-01〜10 ほかをアプリへ、AZ-07/08 を基盤へ、HC-06/08・INT-04・HE-13 を要判断へ）
- 作業種別（C 列）HB-08 → レビュー・合意

### 5. 根拠の補足（149 通り）— `SYNC_note.tsv` → N 列

「根拠の補足 → 対象 WBS ID」の対応。**全行分**入っているので、行ごとに該当する文言を N 列へ入れる（変わらない行は同じ値になる）。省略・吸収した行は `【省略・吸収】…` で始まる。

### 6. 人日（全 463 行）— `SYNC_effort.tsv` → H 列

**H 列と SSOT に 21 行の食い違いがあった**（A-1.7 / B-1.1〜B-5 / D-9.3b / HD-01 / HE-16 / HE-19c / HE-19d / HE-20 / HB-F-INT-01・02・03・05・06）。**すべて Excel 側の編集を正とし、SSOT を Excel に合わせた**ので、これらの行は H 列を変えない。

そのほかの人日変更: E-3 4→1 / E-6 3→1 / D-19 5→1 / HA-18 1→2。

### 7. 概要・成果物の変更（78 行）— `SYNC_text.tsv` → F / G 列

「WBS ID / 列 / 新しい値」。列が「概要」なら F、「成果物と完了条件」なら G を**丸ごと置き換える**（追記済みの全文が入っている）。成果物の先頭に `→ 収載先: ○○設計書。` が付く行が 51 行ある。

### 8. 並び順（全 463 行）— `SYNC_order.tsv`

1. 一時列（例: U 列）に「並び順」の番号を VLOOKUP で入れる
2. データ行 7 行目以降を U 列の昇順で並べ替える
3. **U 列を削除する**

主な移動: A 系は調査計画 13 行 → 対象外 10 行の順 / ADM-09 を AUTH-16 の後（MFA のまとまり）/ D-13 を AUTH-20 の後 / HA-10 を AUTH-22 の後 / D-5 を AZ-08 の後 / AZ-03・04 を ADM-11・12 の後 / AZ-01・02・HD-16 を PROV-17 の後 / HD-05・HD-18・HJ-15・HD-17・D-8b を PROV-08 の後 / HD-24・BAT-01・BAT-09・HJ-04・D-8a・D-8c を PROV-11 の後 / BAT-03・BAT-02・HA-08 を HD-02 の後 / HA-26・BAT-04・05・07・D-12・G-1.5・HF-05 を HE-19d の後 / HG-04〜06・D-9.2・HC-10・E-7・D-20・HD-15 を HA-07 の後 / G-4.1・G-4.2・HJ-17〜19 を HA-05 の後 / 吸収された行を吸収先の直後（AUTH-03→AUTH-02、PROV-01→AUTH-07、BAT-08→ADM-19、HJ-16→HJ-03、D-11・HE-17→HJ-14、HE-10→HE-09、HE-02→HE-01、HD-23→HD-22、D-14→AUTH-21、HA-15・HE-08→HA-18）。

### 9. 検算

| 項目 | 期待値 |
|---|---:|
| データ行数 | **463** |
| H 列合計（全量） | **735.25** |
| スコープ = 対象 の H 合計 | **562.25** |
| スコープ = 対象外：省略・吸収 | 123.5 |
| スコープ = 対象外：認証製品を 1 つに統合 | 29.5 |
| スコープ = 対象外：災害対策をやらない | 20 |
| 対象のうち 案② 基盤 / アプリ / 要判断 | 475.5 / 64.75 / 22 |
| 対象のうち 案③ 基盤 / アプリ / 共同 | 230.5 / 318.75 / 13 |

---

## サイズが問題になる場合

SYNC_new_rows.tsv（20KB）/ SYNC_note.tsv（20KB）/ SYNC_text.tsv（16KB）が大きい場合は、行の途中で 2 分割して渡せる（ヘッダ行を付ければ独立に使える）。SYNC_class / effort / order / name / delete は 12KB 以下。
