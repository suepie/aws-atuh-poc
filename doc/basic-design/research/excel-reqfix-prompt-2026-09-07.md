# Excel 反映指示 — 要件マッピングのレビュー対応（2026-09-07）

以下をそのまま Excel 担当（エージェント）へ渡す。

---

## 依頼

`doc/excel/認証タスク.xlsx` を、SSOT（`doc/basic-design/research/wbs-text/SHEET_*.tsv`）の最新状態に合わせて更新してください。7 シートの表を全面置換し、3 シートの見出し固定文を書き換えます。数式・書式・フィルタは壊さないでください。

- **根拠**: [req-mapping-fix-2026-09-07.md](req-mapping-fix-2026-09-07.md)（決定・追加・矛盾修正・§7 担当 6 者分割）
- **差分の内訳**（確認用、作業には使わない）: `wbs-text/SYNC_reqfix_2026-09-07.tsv`（200 件）/ `wbs-text/SYNC_owner_split_2026-09-07.tsv`（188 件）
- **入力**: `wbs-text/SHEET_*.tsv`（UTF-8・タブ区切り・1 行目ヘッダ・列順は Excel と同じ）
- **大きい 2 ファイルは分割版を使う**（読み取りが途中で切れるのを防ぐため）。分割版は各ファイルとも 1 行目がヘッダで、_1 と _2 を上下に continue して 1 枚の表になる
  - `SHEET_基本設計_1.tsv`（79 行 / 先頭 HB-F-AUTH-02〜HH-06）＋ `SHEET_基本設計_2.tsv`（79 行 / HA-04〜HK-09）＝ 158 行
  - `SHEET_要件マッピング_1.tsv`（105 行 / FR-AUTH-001-a〜FR-INT-007-a）＋ `SHEET_要件マッピング_2.tsv`（104 行 / FR-INT-007-b〜NFR-MIG-005）＝ 209 行
  - 分割前の `SHEET_基本設計.tsv` / `SHEET_要件マッピング.tsv` も同じ内容で残してある（読めるならそちらでよい）
- **原則**: TSV の列順で全面置換。見出し行より上の固定文は残す（§3 で指定した 3 シートだけ書き換える）。行を削らず人日を消さない。**H 列の人日を手で直さない**（SSOT が正。食い違いがあれば報告して止める）

## 1. 全面置換（7 シート）

| シート | TSV | ヘッダ行 | 期待する行数（ヘッダ除く） | 検算 |
|---|---|:-:|---:|---|
| WBS(基本設計) | SHEET_基本設計_1.tsv ＋ _2.tsv | 6（A=ID … S=機能ID の 19 列） | 158 | 人日 全量 233.5 / スコープ「対象」220.5 |
| WBS(詳細設計_製造) | SHEET_詳細設計_製造.tsv | 5（A=ID … I=内訳 の 9 列） | 181 | 詳細設計 184 / 製造 276.5 |
| WBS(テスト) | SHEET_テスト.tsv | 5（A=区分 B=ID … I=前提 の 9 列） | 94 | 人日 345 |
| 要件マッピング | SHEET_要件マッピング_1.tsv ＋ _2.tsv | 1 | 209 | **列を 12 → 15 に増やす**（§2） |
| 機能名一覧 | SHEET_機能名一覧.tsv | 1（A=系統 … J=うち対象 の 10 列） | 140 | 集計行（表の下の固定値 3 行）があれば SUM に置き換える |
| 機能グループ一覧 | SHEET_機能グループ一覧.tsv | 1（5 列） | 33 | — |
| サマリ(工程別) | SHEET_サマリ工程別.tsv | 1（8 列） | 83 | 合計列の総和 **1026** |

手順は各シート共通:
1. ヘッダ行より下を全部消す（行削除。フィルタ・条件付き書式の範囲は後で広げる）
2. TSV をヘッダ行の次の行から貼る（値として。改行入りセルは折り返し）
3. ヘッダ行の書式（フォント・罫線・折り返し・列幅）を貼った行に揃える
4. オートフィルタの範囲を最終行まで広げる
5. 検算列の数値が上表と一致することを確認する

## 2. 要件マッピングの列（12 → 15）

現在の 12 列: 要件ID / 種別 / 要件実現対象 / 構築場所 / 担当 / 項目 / 概要 / ① 基本設計 / ② 詳細設計 / ③ 製造 / ④ テスト / 状況

新しい 15 列（TSV の順）: **要件ID / 種別 / 優先度 / 要件実現対象 / 構築場所 / 担当 / 担当の理由 / 項目 / 概要 / ① 基本設計 / ② 詳細設計 / ③ 製造 / ④ テスト / 状況 / Keycloak-IdP 側**

- 「優先度」「担当の理由」「Keycloak-IdP 側」の 3 列が新規。ヘッダ行を TSV の 1 行目で置き換えてから貼る
- 担当の値は 6 種類: インフラ（本基盤）/ アプリ（Keycloak-IdP）/ アプリ（業務アプリ）/ アプリ（管理画面・idm-api）/ 顧客（顧客IdP）/ 他組織（境界・共通基盤）。対象外は「—」
- 要件ID に枝番付き（例 `FR-AUTH-001-a` / `-b`）が 46 行ある。1 要件を担当ごとに分けた行なので、そのまま貼る
- 状況の集計（貼った後の確認）: 全工程あり 114 / 🔺 一部の工程のみ 10 / アプリ側で実施（3 種計）42 / 顧客側で実施 3 / 他組織で実施 8 / Phase 1 では作らない（2 種計）22 / 別シートで管理 8 / 確定 1 / 要件確認 1 ＝ 209

## 3. 見出しの固定文（3 シート、手で書き換え）

**WBS(基本設計) A1〜A5**: 5 行とも消し、次の 2 行に差し替える（A1・A2）。

> WBS ① 基本設計 — 全量 233.5 人日 / 対象 220.5 人日 / 158 行（「スコープ」列で「対象」を絞れば今やる分。対象外 13.0 ＝ 省略・吸収 7 行 10.0 ＋ Keycloak-IdP 側の責務 3 行 3.0）
>
> ★前提（2026-09-07）: Keycloak-Broker と Keycloak-IdP の 2 台構成。Keycloak-IdP は ROSA の引き渡しまでが本基盤、以降はアプリ責務（D-21 責任表）。停止の伝播は作らず、セッション上限 12 時間で受容。担当・スコープ・当初想定の語彙は変更なし

A3〜A5 は空にする（行は残してよい。ヘッダ行 6 の位置を動かさない）。

**WBS(詳細設計_製造) A1〜A4**: A1 を次に差し替え、A2〜A4 は空にする。

> WBS ② 詳細設計 184 人日 ＋ ③ 製造 276.5 人日 — 181 行（機能グループ → 機能名 → 工程の順。出典: req-mapping-fix-2026-09-07.md）

**WBS(テスト) A1〜A4**: A1 を次に差し替え、A2〜A4 は空にする。

> WBS ④ テスト — 345 人日 / 94 項目（結合 17 / 総合 29 / 実機 5 / 性能 7 / 可用性・DR 4 / セキュリティ 10 / 運用 12 / 受入 4 / テスト共通 6。単体試験は ②③ に計上。出典: req-mapping-fix-2026-09-07.md）

## 4. 検収（全部通ったら完了）

1. 7 シートの行数と人日が §1 の表と一致する
2. 要件マッピングの ①〜④ に入っている ID が、WBS 3 シートのいずれかに存在する（存在しない ID は 0 件のはず）
3. 機能名一覧の 行数 / 人日 / うち対象 が WBS(基本設計) の集計と一致する（例: F-AUTH-01 = 2 行 2.5・うち対象 0.5 / F-AUTH-29 = 2 行 3 / F-AUTH-09 = 3 行 4.5）
4. サマリ(工程別) の各行が WBS 3 シートの機能名別の合計と一致し、合計列の総和が 1026
5. 機能グループ・機能名が 4 シート（基本設計 / 詳細設計_製造 / テスト / サマリ）でマスタ（機能名一覧）に存在する
6. ID の重複が無い（WBS 3 シート横断、要件ID）
7. 見出し 3 シートに旧値（754 / 704 / 473 / 743 / 429 / Realm 統合 / 災害対策をやらない）が残っていない
8. 並び順（機能グループ → 機能名）に逆転・分断が無い（基本設計 / 詳細設計_製造 / 機能名一覧 / 機能グループ一覧 / サマリ）。**テストは「区分 → 機能グループ」順**なので、区分をまたぐ機能グループの再出現は正常

## 5. 報告してほしいこと

- 各シートの行数・人日（貼った後の実測値）
- 検収 1〜7 の結果
- 貼り付けで崩れた書式・数式があればその場所
- 手で直したくなった箇所があっても直さず、場所と理由だけ報告する（SSOT 側で直してから再反映する）

---

## 追補（2026-09-07、1 回目の反映の報告を受けて）

**指摘は正しい。** 不足していた 2 行は次のとおりで、SSOT に存在する（`D-22` 0.5 ＋ `HH-06` 1.0 ＝ 1.5 人日、156 行 → 158 行）。

| ID | 分類 | 作業種別 | 機能グループ | 機能名 | 項目 | 人日 | スコープ | 状態 | 機能ID |
|---|---|---|---|---|---|---:|---|---|---|
| D-22 | D 判断 | 調査・計画 | 同意管理 | 利用規約への同意取得と記録 | 同意取得の要否と方式の判断 | 0.5 | 対象 | **要件確認** | F-AUTH-28 |
| HH-06 | HH 運用設計 | 設計書の執筆 | 監視・検知 | 不正兆候の検知 | 運用設計：不正兆候の検知項目と通知 | 1 | 対象 | 未着手 | F-INT-08 |

**原因は 2 つあった。**

1. **並び順の不具合（こちら側の誤り、修正済み）**: この 2 行が機能グループ順を外れてファイル末尾に付いていた。本来は 同意管理 が「ログイン画面・エラー画面」の後、監視・検知 が「鍵・証明書管理」の後。**SSOT を並べ替えて修正した**（基本設計は D-22 が 36 行目、HH-06 が 79 行目に移動。詳細設計_製造 の DDG-14 / MKH-07、機能名一覧・機能グループ一覧・サマリの末尾 4 行も同じ規則で整えた）。**行数・人日・紐付けは一切変わっていない**
2. **ファイルが読み取り上限で切れる**: 上の分割版（`_1` / `_2`）を追加した。以後はそちらを使う

**やり直してほしいこと**: 基本設計と要件マッピングを**分割版で貼り直す**（並び順が変わっているため、追加 2 行だけを足すのではなく全面置換）。他の 5 シートも並び順を整えたので同様に貼り直す。数値の期待値は §1 の表のまま（① 158 行 233.5 / 対象 220.5、合計 1026）。

**1 回目に反映済みの内容（追加 9 行・修正 2 件）はすべて正しい**ので、全面置換すれば結果は同じになる。

---

## 6. 追加シート「要件・機能対応表」（報告用、2026-09-07 追加）

報告資料として、**要件と機能の対応表**を新しいシートで作ってください。既存シートは変更しません。

- **シート名**: `要件・機能対応表`（位置は「要件マッピング」の右隣）
- **入力**: `wbs-text/SHEET_要件機能マッピング_1.tsv`（158 行 / FR-AUTH-001-a〜FR-INT-006）＋ `_2.tsv`（157 行 / FR-INT-006〜（横断））＝ **315 行**。分割前の `SHEET_要件機能マッピング.tsv` も同じ内容
- **列（8 列）**: 要件ID / 要件 / 要件の概要 / 機能ID / 機能 / 機能の概要 / 担当 / 状況
- **報告時の見せ方**: 主役は **要件 / 機能 / 要件の概要 / 機能の概要** の 4 列。要件ID・機能ID・担当・状況の 4 列は**非表示**にする（列削除ではなく非表示。絞り込みに使う）
- **1 要件が複数の機能で実現される場合は行を分けてある**（209 要件 → 315 行）。要件ID と要件・要件の概要が同じ値で連続するので、**要件ID・要件・要件の概要の 3 列だけ縦方向に結合**するか、罫線で区切りを見せると読みやすい
- 末尾 4 行は要件ID が `（横断）` で、特定の要件に紐づかない共通の作業（設計書の総則・トレーサビリティ・設計レビュー・顧客向け説明資料）。**削除しない**

### 検収（追加分）

9. 行数 315 / 列 8。要件ID の異なり数が 209 ＋ 横断 1 ＝ 210
10. 機能ID がすべて 機能名一覧 に存在する（108 機能）
11. 「要件の概要」が 要件マッピング の同じ要件IDの「概要」と一致、「機能の概要」が 機能名一覧 の同じ機能IDの「概要」と一致
