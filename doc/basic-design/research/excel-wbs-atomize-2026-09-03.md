# 依頼: WBS ②詳細設計の原子分解の反映と ③構築への軸付与（案B 実施）

作成: 2026-09-03 ／ 対象: doc/excel/認証タスク.xlsx ／ このファイル全体をエクセル担当エージェントにそのまま渡す想定

## 0. 背景と方針（先に読む）

② WBS(詳細設計_製造) の粒度を ① WBS(基本設計) と同じ 0.5〜3 人日に揃える決定（案B）を実施した。
分解の結果とルールは **[wbs-detailed-design-atomization-2026-09-03.md](wbs-detailed-design-atomization-2026-09-03.md)**（以下「分解ノート」）が SSOT。
**このファイルは手順だけを書く。行の中身はすべて分解ノートの表からコピーする**こと（自分で行を創作しない）。

前提:
- 前回指示（[excel-wbs-align-2026-09-03.md](excel-wbs-align-2026-09-03.md)）が適用済みであること（②に Q〜U 列がある状態）
- **作業前にブックを閉じてコピーを取る**

やること 3 つ:
1. ②工程の 102 行を、分解ノート §3 の **231 の原子行に差し替える**（人日総量 473 は不変）
2. ③工程の 115 行に、分解ノート §4 の表で**機能グループ・担当 2 列・スコープを付与し直す**（前回の「全行基盤チーム」一括値の是正）
3. 行数変化に伴う**数式・フィルタ範囲・タイトルの追随**と検収

## 1. ② 詳細設計 102 行 → 231 行の差し替え

1. 工程列が「② 詳細設計」の行（DDA-01〜DDJ-06、102 行）を削除する。③の行はそのまま。
2. 分解ノート **§3 の表**（DD-A〜DD-J、231 行）を同じ位置（②ブロック）へ挿入する。列の対応:

| ②シートの列 | 入れる値 |
|---|---|
| A 工程 | `② 詳細設計` |
| B ID | 表の「枝ID」 |
| C 分類 | 表の見出し（DD-A 共通・方式の実装仕様 …）をそのまま |
| D 項目 | 表の「項目」 |
| E 概要・完了定義 | 分解ノート §1 ルール 6 の通り: DD-B 枝は「{機能名} の内部設計（処理の流れ・クラス構成・例外処理の実装方式）。振る舞いは①基本設計 {①出典} で確定済み」。その他の枝は親行の概要を継承し枝の観点（項目名）で読み替え |
| F 人日 | 表の「人日」 |
| G 機能グループ ／ H 機能名 | 表の値 |
| J 依存・着手条件 ／ K 成果物種別 ／ L 区分 | 親行の値を継承（親が空なら空） |
| M 状態 | `未着手` |
| N 追加時期 | `v4.0（2026-09-03 原子分解）` |
| O 出典 | `wbs-detailed-design-atomization-2026-09-03.md §3` |
| Q スコープ ／ R 担当（案②） ／ S 担当（案③） ／ T 当初想定 | 表の値（スコープ列の正式表記は「対象」「対象外：認証製品を 1 つに統合」「対象外：災害対策をやらない」） |
| U 人日（按分後） | 数式 `=IF($A6="③ 構築（DU）",$F6*0.6,$F6)` を全データ行に張り直す |

## 2. ③ 構築 115 行への軸付与

分解ノート **§4 の表**の通りに、③の各行の G 機能グループ・Q スコープ・R 案②・S 案③ を上書きする。
- 表で「DU-U6-01〜04, 07〜10, 12〜14」のようにまとめてある行は、その範囲の全 DU に同じ値を入れる
- H 機能名は空欄のままでよい（DU は複数機能を束ねる粒度のため）
- T 当初想定: 機能グループが付いた行は①の同グループから導出（割れたら `新規: 機能追加` を仮置き）、横断は `新規: 自社構築` のまま
- 備考列（P）に分解ノート §4 の備考（「縮小見込み」等）を転記する
- DU-U10-04 / DU-U10-11 は人日 0・状態表記のまま触らない

## 3. 追随修正

1. **タイトル r1**: `WBS ②詳細設計 473 ＋ ③構築 743 — 全量 346 行（② 原子 231 行〔0.5〜3 人日、①と同粒度〕／ ③ DU 107 行=1,044×60%→626 ／ ③ 横断BX 8 行=117）`
2. **★注記**（r3 か r4 に追記）: `★②は 2026-09-03 に①と同じ粒度へ原子分解（102→231 行、総量 473 不変）。出典: wbs-detailed-design-atomization-2026-09-03.md。③の担当・スコープも同日付与（縮小見込み 4 件は②→③突合パスで再見積り）`
3. **範囲の張り直し**（行数が 222 → 約 351 に増える）:
   - オートフィルタ: A5:U〔最終データ行〕
   - KPI H2: `=SUM($U$6:$U$351)` 相当（最終行に合わせる）
   - ダッシュボード r39 の B39/D39（`DD*` の SUMIFS/COUNTIFS）と r40 の B40/D40（`③*` の SUMIF/COUNTIF）の参照範囲を最終行まで拡張
   - ウィンドウ枠固定は A6 のまま
4. **④ WBS(テスト) のスコープ検算**（マーク漏れの是正）: ④の「対象」人日の期待値は **388**（429 − 災害対策 16 − Realm 統合 25）。現状マークが不足している場合、[dr-scope-decision-2026-09-02.md §4-2](dr-scope-decision-2026-09-02.md) と [realm-consolidation-2026-09-02.md §2](realm-consolidation-2026-09-02.md) を根拠に、該当行（再構築手順の実証・復旧時間実測／2 層間・伝播・控え系のテスト）へ `対象外：災害対策をやらない`／`対象外：認証製品を 1 つに統合` を付与する。判断が付かない行はマークせず行 ID を報告に残す。

## 4. 検収チェックリスト

1. ②工程: **231 行・F 列合計 473**（±0）。B 列に重複 ID なし
2. ② スコープ別: 対象 438.5 ／ 対象外：認証製品を 1 つに統合 20.5 ／ 対象外：災害対策をやらない 14
3. ② の 3 人日超は 6 行のみ（DDJ-01〜04・DDJ-05・DDJ-06）
4. ③工程: 機能グループ空欄 0 行（DU-U10-04 の「—」は可）。担当（案③）に `アプリチーム`＝約 30 行・`共同`＝約 10 行が存在する（全行基盤チームでないこと）
5. ③ スコープ別（原値）: 対象外：災害対策 = 40（U8-02/04/10/12）／ 対象外：Realm 統合 = 58（U2-02/10・U3-05/09/10・U8-11）
6. KPI（② H1:H3・④ E1:E3）: 754 ／ 1,216 ／ 429 のまま（全量は不変）
7. ダッシュボード r39: ② = 473・231 件。r43 総計 2,399 のまま
8. ④: スコープ「対象」の人日 ≈ 388（乖離があれば理由を報告）
9. 全シート再計算で #REF! なし、フィルタ範囲が全列・全行を覆う

## 5. 注意

- 行の挿入で②③の境目がずれるため、**③の先頭行（DU-U2-01）の直前に②の 231 行が収まっている**ことを目視確認する
- 分解ノートの表と 1 行でも食い違ったら、創作せず作業を止めて差分を報告する
- サマリ(機能・担当)・サマリーの数式は①参照のため影響なし（触らない）
